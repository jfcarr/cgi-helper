# CGI Helper

Standalone Python script (standard library only) implementing basic CGI (Common Gateway Interface) handling: **src/simple_cgi.py**.

Refer to the `self_test()` function in the script for usage examples.
